# ⚠️ THE CHANGELOG IS CURATED, NOT GENERATED. Without manual-changelog the
# packager builds release notes from the FULL COMMIT BODIES between tags and
# publishes them to GitHub AND CurseForge. This repo is public and its commit
# messages are internal engineering notes — they name people, narrate mistakes,
# and carry Co-Authored-By trailers. All of that reached the public store page.
#
# CHANGELOG.md is user-facing. Write it for someone deciding whether to update.
manual-changelog: CHANGELOG.md

# BigWigs packager config. Releases go to GitHub AND to CurseForge project
# 1671911 (see the X-Curse-Project-ID note in the .toc). CurseForge is the one
# that matters for updates: WoWUp can INSTALL from a GitHub release but cannot
# UPDATE from one, so a GitHub-only release reaches nobody who already has it.

package-as: GloomsBuildBarn

enable-nolib-creation: no

# Minimap button libraries. The packager fetches these into Libs/ at release
# build time only — they are NOT committed to the repo (kept clean). Locally
# (where Libs/ is absent) MinimapButton.lua falls back to a self-contained button.
externals:
  Libs/LibStub: https://repos.wowace.com/wow/libstub/trunk
  Libs/CallbackHandler-1.0: https://repos.wowace.com/wow/callbackhandler/trunk/CallbackHandler-1.0
  Libs/LibDataBroker-1.1: https://github.com/tekkub/libdatabroker-1-1
  Libs/LibDBIcon-1.0: https://repos.wowace.com/wow/libdbicon-1-0/trunk/LibDBIcon-1.0

# Keep repo-management files out of the packaged addon zip.
ignore:
  - README.md
  - .github
  - .gitignore
  - .pkgmeta
